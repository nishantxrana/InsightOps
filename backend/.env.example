# ============================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================
# MongoDB connection string - Get from MongoDB Atlas or local instance
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/InsightOps?retryWrites=true&w=majority&appName=Cluster0

# ============================================
# SECURITY CONFIGURATION (REQUIRED)
# ============================================
# JWT secret for authentication tokens (min 32 characters)
# Generate: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=your-jwt-secret-key-here-64-characters-long-random-string-example

# Encryption key for sensitive data (64 hex characters = 32 bytes)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=your-32-byte-hex-encryption-key-here-64-characters-total-example

# ============================================
# APPLICATION CONFIGURATION (REQUIRED)
# ============================================
# Node environment: development, staging, production
NODE_ENV=development

# Server port
PORT=3001

# Allowed CORS origins (comma-separated for multiple)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3001

# ============================================
# EMAIL SERVICE (REQUIRED FOR AUTH)
# ============================================
# Brevo (formerly Sendinblue) API key for transactional emails
# Sign up at: https://app.brevo.com
# Used for: Email verification OTPs, password reset OTPs
BREVO_API_KEY=xkeysib-your-api-key-here

# Email sender configuration
FROM_EMAIL=support@yourdomain.com
FROM_NAME=InsightOps

# Email verification secret (min 32 characters)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
EMAIL_VERIFICATION_SECRET=your-email-verification-secret-here-64-characters-long

# Password reset secret (min 32 characters)
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
PASSWORD_RESET_SECRET=your-password-reset-secret-here-64-characters-long